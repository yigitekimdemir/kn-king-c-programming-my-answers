#include <stdio.h>

int main(void)
{

    int radius = 10, pi = 3.14f, area;

    area = 4.0f/3.0f * pi * radius * radius * radius; //calculation formula

    printf("%d", area);

    return 0;
}
